# CMakeLists.txt for libraries.
#

cmake_minimum_required (VERSION 3.25)
add_subdirectory(googletest SYSTEM)
add_subdirectory(wxWidgets SYSTEM)

#set(wxBUILD_SHARED FALSE)
# TODO: Add tests and install targets if needed.
